# Kopieren nach `.env.local` (nicht committen).
# Build-Cache im Projekt, aber eigener Ordner (oft stabiler mit OneDrive als `.next`):
# NEXT_DIST_DIR=.next-cache

# Oder Cache neben dem Projektordner (nicht von OneDrive syncen):
# NEXT_DIST_DIR=../handsplus-next-cache
